import { EntityExistsPipeClass } from './entity-exists.pipe';

describe('EntityExistsPipePipe', () => {
  it('should be defined', () => {
    expect(new EntityExistsPipeClass()).toBeDefined();
  });
});
